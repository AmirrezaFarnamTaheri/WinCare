<UserControl
    x:Class="WinCare.App.Controls.WidgetContainer"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:plugins="using:WinCare.Application.Plugins">

    <StackPanel Spacing="12">
        <TextBlock Text="Extension widgets" Style="{StaticResource SectionHeaderTextStyle}" />
        <ItemsControl x:Name="WidgetsList">
            <ItemsControl.ItemTemplate>
                <DataTemplate x:DataType="plugins:IPluginWidget">
                    <Border Background="{ThemeResource CardSurfaceBrush}"
                            BorderBrush="{ThemeResource BorderSubtleBrush}"
                            BorderThickness="1" CornerRadius="{StaticResource RadiusControl}" Padding="16" Margin="0,0,0,8">
                        <Grid ColumnSpacing="12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0" Spacing="4">
                                <TextBlock Text="{x:Bind Title, Mode=OneTime}" Style="{StaticResource RowTitleTextStyle}" />
                                <TextBlock Text="{x:Bind StatusSummary, Mode=OneTime}" Style="{StaticResource RowSecondaryTextStyle}" MaxLines="4" />
                            </StackPanel>
                            <ContentControl Grid.Column="1" VerticalAlignment="Center" Template="{StaticResource StatusPillTemplate}" Background="{ThemeResource AccentChromeSubtleBrush}" BorderBrush="{ThemeResource AccentChromeBrush}" Content="{x:Bind Category}" Foreground="{ThemeResource TextPrimaryBrush}" />
                        </Grid>
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </StackPanel>
</UserControl>
