<UserControl
    x:Class="WinCare.App.Controls.PageHeaderControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Margin="0,0,0,24">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>

        <StackPanel Spacing="6" VerticalAlignment="Center">
            <StackPanel Orientation="Horizontal" Spacing="12">
                <TextBlock Text="{x:Bind Title, Mode=OneWay}" Style="{StaticResource PageTitleTextStyle}" />
                <Border Background="{ThemeResource AccentChromeSubtleBrush}" 
                        CornerRadius="{StaticResource RadiusControl}" 
                        Padding="8,2" 
                        VerticalAlignment="Center"
                        Visibility="{x:Bind HasBadge, Mode=OneWay}">
                    <TextBlock Text="{x:Bind BadgeText, Mode=OneWay}" 
                               FontSize="11" 
                               FontWeight="SemiBold" 
                               Foreground="{ThemeResource AccentChromeBrush}" />
                </Border>
            </StackPanel>
            <TextBlock Text="{x:Bind Description, Mode=OneWay}" 
                       Style="{StaticResource PageDescriptionTextStyle}" 
                       MaxWidth="850" />
        </StackPanel>

        <ContentPresenter Grid.Column="1" 
                          Content="{x:Bind ActionContent, Mode=OneWay}" 
                          VerticalAlignment="Center" />
    </Grid>
</UserControl>
