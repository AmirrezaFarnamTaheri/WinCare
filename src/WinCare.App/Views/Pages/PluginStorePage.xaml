<Page
    x:Class="WinCare.App.Views.Pages.PluginStorePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WinCare.App.Views.Pages"
    xmlns:vm="using:WinCare.App.ViewModels.Pages"
    SizeChanged="Page_SizeChanged">

    <Grid Padding="32,24,32,32" RowDefinitions="Auto,Auto,Auto,Auto,*" RowSpacing="18">
        <Grid Grid.Row="0" ColumnDefinitions="*,Auto" RowDefinitions="Auto,Auto,Auto" ColumnSpacing="16" RowSpacing="12">
            <StackPanel Grid.Column="0" Spacing="8">
                <TextBlock Text="Extensions" Style="{StaticResource PageTitleTextStyle}" />
                <TextBlock MaxWidth="760" HorizontalAlignment="Left" Text="Add focused capabilities when you need them, and manage the extensions already available to WinCare." Style="{StaticResource PageDescriptionTextStyle}" />
            </StackPanel>

            <AutoSuggestBox x:Name="PluginSearchBox" Grid.Column="1" Width="280" VerticalAlignment="Center" PlaceholderText="Search extensions or authors" QueryIcon="Find" TextChanged="SearchBox_TextChanged" AutomationProperties.AutomationId="PluginSearch" AutomationProperties.Name="Search extensions or authors"/>
            <ComboBox x:Name="CategoryFilter" Grid.Row="1" Grid.ColumnSpan="2" Width="220" HorizontalAlignment="Left" Header="Category" ItemsSource="{x:Bind ViewModel.Categories, Mode=OneWay}" SelectedItem="{x:Bind ViewModel.SelectedCategory, Mode=TwoWay}" AutomationProperties.AutomationId="PluginCategoryFilter" AutomationProperties.Name="Filter extensions by category" />
        </Grid>

        <Expander Grid.Row="1"
                  Header="Catalog status"
                  IsExpanded="False"
                  Visibility="{x:Bind local:PluginStorePage.BoolToVisibility(ViewModel.HasCatalogStatus), Mode=OneWay}"
                  AutomationProperties.AutomationId="PluginCatalogStatus"
                  AutomationProperties.Name="Extension catalog trust and availability">
            <TextBlock Style="{StaticResource RowSecondaryTextStyle}"
                       Text="{x:Bind ViewModel.CatalogStatusMessage, Mode=OneWay}"
                       TextWrapping="Wrap" />
        </Expander>

        <InfoBar Grid.Row="2"
                 IsOpen="{x:Bind ViewModel.HasError, Mode=OneWay}"
                 Severity="Error"
                 Title="Extensions notice"
                 Message="{x:Bind ViewModel.ErrorMessage, Mode=OneWay}"
                 IsClosable="False"
                 AutomationProperties.LiveSetting="Assertive">
            <InfoBar.ActionButton>
                <Button Content="Retry connection" Style="{StaticResource QuietButtonStyle}" Click="RetryCatalog_Click" IsEnabled="{x:Bind ViewModel.CanRefresh, Mode=OneWay}" Visibility="{x:Bind local:PluginStorePage.BoolToVisibility(ViewModel.HasCatalogError), Mode=OneWay}" AutomationProperties.AutomationId="PluginCatalogRetry" AutomationProperties.Name="Retry extension catalog connection" />
            </InfoBar.ActionButton>
        </InfoBar>

        <StackPanel Grid.Row="3" Spacing="8" Visibility="{x:Bind local:PluginStorePage.BoolToVisibility(ViewModel.IsLoading), Mode=OneWay}" AutomationProperties.LiveSetting="Polite">
            <TextBlock Text="Updating your extension library…" Style="{StaticResource RowSecondaryTextStyle}" />
            <ProgressBar IsIndeterminate="{x:Bind ViewModel.IsLoading, Mode=OneWay}" AutomationProperties.Name="Loading extension catalog" />
        </StackPanel>

        <ListView Grid.Row="4"
                  ItemsSource="{x:Bind ViewModel.Plugins, Mode=OneWay}"
                  SelectionMode="None"
                  AutomationProperties.AutomationId="PluginCatalog"
                  AutomationProperties.Name="Extensions list">
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                    <Setter Property="Padding" Value="0,0,0,12"/>
                </Style>
            </ListView.ItemContainerStyle>
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="vm:PluginCardViewModel">
                    <Border Background="{ThemeResource CardSurfaceBrush}"
                            BorderBrush="{ThemeResource BorderSubtleBrush}"
                            BorderThickness="1" CornerRadius="{StaticResource RadiusCardOuter}" Padding="20">
                        <StackPanel Spacing="16">
                            <StackPanel Spacing="8">
                                <Grid ColumnDefinitions="*,Auto" ColumnSpacing="12">
                                    <TextBlock Text="{x:Bind Name}" FontWeight="SemiBold" FontSize="16" Foreground="{ThemeResource TextPrimaryBrush}" TextWrapping="Wrap" VerticalAlignment="Center" />
                                    <Border Grid.Column="1" HorizontalAlignment="Right" Background="{ThemeResource AccentTealSubtleBrush}" BorderBrush="{ThemeResource AccentTealBrush}" BorderThickness="1" CornerRadius="{StaticResource RadiusPill}" Padding="6,2" VerticalAlignment="Center">
                                        <TextBlock Text="{x:Bind StatusBadgeText}" FontFamily="{StaticResource TelemetryFontFamily}" FontSize="11" FontWeight="SemiBold" Foreground="{ThemeResource AccentTealBrush}" TextWrapping="Wrap" />
                                    </Border>
                                </Grid>
                                <TextBlock Text="{x:Bind Description}" Style="{StaticResource RowSecondaryTextStyle}" TextWrapping="Wrap" />
                                <TextBlock Text="{x:Bind PublisherTrustBadgeText}" FontSize="12" Foreground="{ThemeResource TextSecondaryBrush}" TextWrapping="Wrap" />
                                <StackPanel Spacing="3" Margin="0,2,0,0">
                                    <TextBlock Text="{x:Bind Category}" FontSize="12" Foreground="{ThemeResource TextSecondaryBrush}" TextWrapping="Wrap" />
                                    <TextBlock Text="{x:Bind Author}" FontSize="12" Foreground="{ThemeResource TextSecondaryBrush}" TextWrapping="Wrap" />
                                    <TextBlock Text="{x:Bind Version}" FontSize="12" FontFamily="{StaticResource TelemetryFontFamily}" Foreground="{ThemeResource TextSecondaryBrush}" TextWrapping="Wrap" />
                                </StackPanel>
                            </StackPanel>

                            <StackPanel Orientation="Horizontal" Spacing="8">
                                <Button Content="Review" Click="DetailsButton_Click" Tag="{x:Bind}" AutomationProperties.Name="View extension details"/>
                                <Button Content="Add" Visibility="{x:Bind local:PluginStorePage.BoolToVisibility(CanInstall), Mode=OneTime}" Click="InstallButton_Click" Tag="{x:Bind}" Style="{StaticResource AccentButtonStyle}" AutomationProperties.Name="Review and install extension"/>
                                <Button Content="Enable" Visibility="{x:Bind local:PluginStorePage.BoolToVisibility(CanEnable), Mode=OneTime}" Click="EnableButton_Click" Tag="{x:Bind}" Style="{StaticResource AccentButtonStyle}" AutomationProperties.Name="Enable extension"/>
                                <Button Content="Disable" Visibility="{x:Bind local:PluginStorePage.BoolToVisibility(CanDisable), Mode=OneTime}" Click="DisableButton_Click" Tag="{x:Bind}" AutomationProperties.Name="Disable extension"/>
                                <Button Content="Uninstall" Visibility="{x:Bind local:PluginStorePage.BoolToVisibility(CanUninstall), Mode=OneTime}" Click="UninstallButton_Click" Tag="{x:Bind}" AutomationProperties.Name="Uninstall extension"/>
                            </StackPanel>
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <InfoBar Grid.Row="4" Margin="12" VerticalAlignment="Top" IsOpen="{x:Bind ViewModel.IsEmpty, Mode=OneWay}" IsClosable="False" Severity="Informational" Title="No extensions found" Message="Try a different search or category. Installed extensions remain available even when the online catalog cannot be reached." AutomationProperties.LiveSetting="Polite" />
    </Grid>
</Page>
