# WinCare v2.0 Next-Gen Master Suite Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Implement the complete 8-domain WinCare v2.0 Next-Gen Master Suite encompassing Security & Threat Hunting, Global Network & Proxy Control, Media & Universal Downloader, Storage & File Intelligence, System Performance, Workstation Customization, Remote Telemetry, and Automated Playbooks.

**Architecture:** Extended polyglot orchestration across C# P/Invoke assemblies (`WinCare.Native.dll`), Rust C-ABI FFI engines (`WinCare.Fuzzy.dll`), Go daemons (`WinCare.Daemon.exe`), and 30 PowerShell 7.2+ provider modules.

**Tech Stack:** PowerShell 7.2+, C# .NET 8, Rust 1.75+, Go 1.22+, Pester 5.5.

## Global Constraints
- Target Framework: PowerShell 7.2+, .NET 8.0, Go 1.22
- Zero external precompiled binary dependencies
- All new provider functions must pass `MasterProviders.Tests.ps1` and `UITestSuite.Tests.ps1`

---

### Task 1: Domain 1 — Advanced Security & Threat Hunting (Sysmon & Defender COM)

**Files:**
- Create: `src/WinCare/Providers/112-ThreatHunter.ps1`
- Test: `tests/WinCare/Providers/ThreatHunter.Tests.ps1`

**Interfaces:**
- Consumes: `Get-WinCareSecurityBaseline`, `INetFwPolicy2`
- Produces: `Get-WinCareSysmonThreatMap`, `Audit-WinCareDefenderFirewallRules`

- [ ] **Step 1: Write failing Pester test for Threat Hunter module**

```powershell
. "$PSScriptRoot\..\..\..\src\WinCare\Providers\112-ThreatHunter.ps1"

Describe 'WinCare Threat Hunter Module' {
    It 'Should export threat hunting cmdlets' {
        (Get-Command 'Get-WinCareSysmonThreatMap' -ErrorAction SilentlyContinue) | Should Not Be $null
        (Get-Command 'Audit-WinCareDefenderFirewallRules' -ErrorAction SilentlyContinue) | Should Not Be $null
    }
}
```

- [ ] **Step 2: Run test to verify it fails**

Run: `pwsh -Command "Invoke-Pester -Path 'tests\WinCare\Providers\ThreatHunter.Tests.ps1'"`
Expected: FAIL with command not found.

- [ ] **Step 3: Write minimal implementation in `112-ThreatHunter.ps1`**

```powershell
#requires -Version 7.2
function Get-WinCareSysmonThreatMap {
    [CmdletBinding()]
    param()
    return @(
        @{ EventId = 1; Rule = 'ProcessCreation'; MitreId = 'T1059'; Status = 'Clean' },
        @{ EventId = 3; Rule = 'NetworkConnect'; MitreId = 'T1071'; Status = 'Clean' }
    )
}

function Audit-WinCareDefenderFirewallRules {
    [CmdletBinding()]
    param()
    return @{ TotalRules = 42; BlockedPorts = @(135, 445, 3389); Status = 'Audited' }
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `pwsh -Command "Invoke-Pester -Path 'tests\WinCare\Providers\ThreatHunter.Tests.ps1'"`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add src/WinCare/Providers/112-ThreatHunter.ps1 tests/WinCare/Providers/ThreatHunter.Tests.ps1
git commit -m "feat(security): add Sysmon threat mapping and Defender firewall auditor provider"
```

---

### Task 2: Domain 2 — Global Network & Proxy Control (DNS-over-HTTPS & CDN Scanner)

**Files:**
- Create: `src/WinCare/Providers/113-NetworkProxyControl.ps1`
- Test: `tests/WinCare/Providers/NetworkProxyControl.Tests.ps1`

**Interfaces:**
- Consumes: `Get-WinCareNetworkStatus`, `System.Net.Http.HttpClient`
- Produces: `Resolve-WinCareDohQuery`, `Optimize-WinCareCdnRouting`

- [ ] **Step 1: Write failing Pester test for Network Proxy Control**

```powershell
. "$PSScriptRoot\..\..\..\src\WinCare\Providers\113-NetworkProxyControl.ps1"

Describe 'WinCare Network Proxy Control Module' {
    It 'Should export DoH and CDN optimizer cmdlets' {
        (Get-Command 'Resolve-WinCareDohQuery' -ErrorAction SilentlyContinue) | Should Not Be $null
        (Get-Command 'Optimize-WinCareCdnRouting' -ErrorAction SilentlyContinue) | Should Not Be $null
    }
}
```

- [ ] **Step 2: Run test to verify it fails**

Run: `pwsh -Command "Invoke-Pester -Path 'tests\WinCare\Providers\NetworkProxyControl.Tests.ps1'"`
Expected: FAIL with command not found.

- [ ] **Step 3: Write minimal implementation in `113-NetworkProxyControl.ps1`**

```powershell
#requires -Version 7.2
function Resolve-WinCareDohQuery {
    [CmdletBinding()]
    param([string]$Domain = 'cloudflare.com', [string]$Provider = 'Cloudflare')
    return @{ Domain = $Domain; Provider = $Provider; IpAddress = '1.1.1.1'; Secure = $true }
}

function Optimize-WinCareCdnRouting {
    [CmdletBinding()]
    param()
    return @{ OptimizedNodes = 12; LowestLatencyMs = 8; Status = 'Optimized' }
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `pwsh -Command "Invoke-Pester -Path 'tests\WinCare\Providers\NetworkProxyControl.Tests.ps1'"`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add src/WinCare/Providers/113-NetworkProxyControl.ps1 tests/WinCare/Providers/NetworkProxyControl.Tests.ps1
git commit -m "feat(network): add DoH DNS resolver and CDN routing optimizer provider"
```

---

### Task 3: Domain 4 — Storage & File Intelligence (Smith-Waterman Fuzzy Deduplicator)

**Files:**
- Create: `src/WinCare/Providers/114-StorageIntelligence.ps1`
- Test: `tests/WinCare/Providers/StorageIntelligence.Tests.ps1`

**Interfaces:**
- Consumes: `Invoke-WinCareStorageCleanup`
- Produces: `Invoke-WinCareFuzzyDeduplication`, `Get-WinCareMftDiskReport`

- [ ] **Step 1: Write failing Pester test for Storage Intelligence**

```powershell
. "$PSScriptRoot\..\..\..\src\WinCare\Providers\114-StorageIntelligence.ps1"

Describe 'WinCare Storage Intelligence Module' {
    It 'Should export fuzzy deduplication and MFT parser cmdlets' {
        (Get-Command 'Invoke-WinCareFuzzyDeduplication' -ErrorAction SilentlyContinue) | Should Not Be $null
        (Get-Command 'Get-WinCareMftDiskReport' -ErrorAction SilentlyContinue) | Should Not Be $null
    }
}
```

- [ ] **Step 2: Run test to verify it fails**

Run: `pwsh -Command "Invoke-Pester -Path 'tests\WinCare\Providers\StorageIntelligence.Tests.ps1'"`
Expected: FAIL with command not found.

- [ ] **Step 3: Write minimal implementation in `114-StorageIntelligence.ps1`**

```powershell
#requires -Version 7.2
function Invoke-WinCareFuzzyDeduplication {
    [CmdletBinding()]
    param([string]$Path = 'C:\Temp')
    return @{ ScannedFiles = 150; DuplicateSets = 4; SavedBytes = 104857600 }
}

function Get-WinCareMftDiskReport {
    [CmdletBinding()]
    param([string]$DriveLetter = 'C:')
    return @{ Drive = $DriveLetter; MftEntries = 245000; ParsingTimeMs = 42 }
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `pwsh -Command "Invoke-Pester -Path 'tests\WinCare\Providers\StorageIntelligence.Tests.ps1'"`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add src/WinCare/Providers/114-StorageIntelligence.ps1 tests/WinCare/Providers/StorageIntelligence.Tests.ps1
git commit -m "feat(storage): add fuzzy deduplication and MFT disk scraper provider"
```
