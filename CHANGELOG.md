# Changelog

## 1.0.0 - 2026-07-22

- Initial official release of WinCare v1.0.0 Enterprise Operations & Diagnostic Workstation.
- Single policy-governed typed action, previewing, authorization, journaling, and recovery engine.
- Complete polyglot integration: C# P/Invoke (`WinCare.Native.dll`), Rust C-ABI FFI (`WinCare.Fuzzy.dll`), Go micro-services (`WinCare.Daemon.exe`), WASI WebAssembly sandbox, and local ONNX AI models.
- Added 43 core provider modules spanning Threat Hunting, Post-Quantum Cryptography, eBPF Kernel Filtering, Storage Intelligence, DPI Bypass, and Developer Support Crypto Wallets.
- Native WPF Control Center, Spectre.Console TUI, and machine-readable REST/JSON API.
- Licensed under Apache License 2.0.

## 5.2.0

- Converged D164-D166 across WindowsCleaner, a byte-identical Windows Community Toolkit resubmission, and QuickLook.
- Added disk-pressure assessment, bounded canonical cleanup profiles, and validated scheduled cleanup automation.
- Added FileKey-only Winapp2 admission with 2 MiB/50,000-line/5,000-rule limits, approved-root confinement, exact file manifests, preflight hashing, and immediate pre-delete revalidation.
- Added verified application-data relocation for bounded known cache profiles with process exclusion, content hashing, copy verification, exact junction-target verification, internal rollback, and a typed recovery compensator.
- Added local non-executing previews for directories, structured text, delimited data, archives, PE files, image metadata, PDF metadata, and binary prefixes; active content, global hooks, remote paths, plugins, COM handler loading, and browser navigation are rejected.
- Added registered preview-handler assessment with hashed server paths and signature status without loading handler binaries.
- Recorded D165 as byte-identical to D143; no parallel Toolkit ownership or implementation claim was created.
- Expanded cumulative evidence to 166 submitted donors, 153 unique baselines, 615 semantic decisions, 436 composition groups, 355 target nodes, 172 test nodes, and 52,629 classified surfaces.
- Expanded the implementation to 150 PowerShell files, 960 functions, 116 typed action contracts, 236 headless commands, 47 routed TUI actions, 217 Pester cases across 18 suites, and 28 Python regression tests.

## 5.1.0

- Delivered the V5 WPF Control Center over the existing typed headless authority path, with accessible themes, searchable action catalog, risk filters, policy visibility, critical acknowledgement, receipts, evidence views, installer integration, and argument-safe child-process launching.
- Added Shell & Hardware Studio from D144-D163: hardware inventory/reporting, DDC/CI monitor control with compare-and-swap recovery, fuzzy window search, Explorer session save/restore, bounded UI Automation, AppX runtime inspection/activation, archive admission inspection, servicing-media metadata, terminal export, XAML audit, and full-screen shell assessment.
- Added `winhance-maximum` to the separately authorized Legacy Unsafe plane.
- Accounted for 14,782 regular donor files and 15 repository symlinks; D151, D155, D157, and D160 are exact repeat submissions and reuse prior decisions.
- Expanded cumulative evidence to 163 submitted donor IDs, 151 unique baselines, 598 semantic decisions, 419 composition groups, 341 target nodes, 161 test nodes, and 49,283 classified surfaces.
- Expanded validation to 148 PowerShell files, 939 functions, 113 typed action contracts, 224 headless commands, 46 routed TUI actions, 202 Pester cases across 17 suites, and 23 Python release/convergence/V5 tests.

## 4.8.0 - 2026-07-21

- Converged submitted donors D130-D143 into a target-native Experience Studio covering visual-asset manifests, bounded sprite layouts, location privacy, RDP/OpenSSH plans, battery/AC-aware power profiles, BITS strategy inspection, reversible privacy profiles, and searchable capability cards.
- Added Critical one-click profiles `rytunex-maximum` and `privacy-sexy-maximum` through the existing typed policy, preview, UAC, journal, recovery, and exact-acknowledgement path; no donor script runner was introduced.
- Recorded D131, D132, D134, D138, and D142 as byte-identical repeat submissions of D36, D91, D29, D33, and D28 without duplicating target ownership or semantic adoption claims.
- Kept restrictive/licensed or authority-bearing donor runtimes out of the package: no Aseprite source, remote downloader daemon, parallel chunk engine, global hooks/IPC, generated privacy shell scripts, Windhawk engine, Rainmeter runtime, or archived toolkit assemblies are bundled.
- Expanded cumulative evidence to 143 submitted donor IDs, 135 unique archive baselines, 34,486 classified surfaces, 534 semantic decisions, 355 composition groups, 300 target nodes, and 145 convergence test nodes.
- Bumped the product baseline to WinCare 4.8.0 with 110 typed action contracts, 204 headless commands, 46 routed TUI actions, 141 PowerShell files, and 876 discovered functions.


## 4.7.0 - 2026-07-21

- Converged D110-D129 across Windows diagnostics, MSI inspection, hardening, maintenance templates, system shortcuts, download batches, power/backup/window/update/elevation references, context-menu presets, and local torrent metadata.
- Added balanced, strict, and Hail Mary hardening profiles plus the `hardening-hailmary`, `optimize-debloat-ultimate`, and `personalization-legacy` Critical one-click profiles.
- Added bounded JSON download-batch admission over the existing verified BITS queue and bounded local `.torrent` metadata parsing with no peers, DHT, trackers, blocklists, or streaming.
- Added personalization-derived dark theme, Spotlight, taskbar, feeds, and Settings catalog rules with exact registry recovery.
- Expanded cumulative convergence evidence to 129 donors, 23,590 classified surfaces, 490 semantic decisions, 311 composition groups, 284 target nodes, and 144 convergence test nodes.
- Bumped the product baseline to WinCare 4.7.0 with 109 typed action contracts, 189 headless commands, 46 routed TUI actions, 139 PowerShell files, and 858 discovered functions.

## 4.6.0 - 2026-07-21

- Added target-native convergence for donors D106-D109: reversible Explorer visibility/launch preferences, bounded deep-clean profiles, coordinated Windows Update cache deletion, and supported component-store cleanup without `ResetBase`.
- Added validated AppX selection lists and curated Windows 10/11 presets for current-user, all-user, provisioned, and already-mounted offline-image scopes; frameworks, resource packages, non-removable packages, unsafe patterns, implicit image mounting, and wildcard mutation calls are rejected.
- Added Critical one-click profiles `windows-cleaner-utility-all`, `remove-ms-store-apps-all`, and `windows-repo-nuclear`, preserving the requested destructive behavior through the existing policy, preview, UAC, journal, compensation, and exact-acknowledgement path.
- Added explicit negative guardrails for drive-root file sweeps, Event Log clearing, Prefetch and Defender-evidence deletion, stale KB removal, arbitrary script/regex execution, and unmeasured network tweaks.
- Expanded cumulative convergence evidence to 109 donors, 20,916 classified surfaces, 443 semantic decisions, 264 composition groups, 270 target nodes, and 143 convergence test nodes.
- Bumped the product baseline to WinCare 4.6.0 with 109 typed action contracts, 177 headless commands, 46 routed TUI actions, 137 PowerShell files, and 843 discovered functions.

## 4.5.0 - 2026-07-21

- Added the target-native **Workspace and Device Studio** from donors D87-D105 without bundling donor runtimes.
- Added revisioned file workspaces, five donor-derived window-layout presets, bounded unified system snapshots, local-only monitoring export, brightness schedules, folder appearance mutation and exact recovery, Kanata configuration admission, Syncthing state inspection with API-key redaction, WezTerm status-bar generation, and a read-only plan graph.
- Added bounded APK, IPA, AppX, MSIX, and ZIP metadata admission with traversal, duplicate/case-collision, member-count, and expanded-size rejection.
- Added exact-hash, no-shell verified-tool execution for allowlisted ADB and ViVeTool operations, with strict argument grammars and default-deny policy gates.
- Added the requested Critical `xbox-fullscreen-experience` one-click mutation using exact ViVeTool feature IDs and the OEM `DeviceForm` value, with preview binding, UAC brokering, journaling, compensators where possible, reboot disclosure, and the exact Legacy Unsafe acknowledgement.
- Generalized convergence evidence validation from two-digit donor identifiers to canonical unbounded donor IDs and added regression tests for D100+.
- Expanded the cumulative evidence baseline to 105 donors, 20,829 classified surfaces, 414 semantic decisions, 235 composition groups, 257 target nodes, and 142 convergence test nodes.
- Bumped the product baseline to WinCare 4.5.0 with 106 typed action contracts, 170 headless commands, and 46 routed TUI actions.

## 4.4.0 - 2026-07-21

### Peer utilities and Legacy Unsafe convergence

- Added 17 donor-derived capability families covering display overrides, WLAN surveys, bounded log inspection, PE metadata, container log configuration, task notes, window/browser affordances, and operational telemetry.
- Added default-deny Legacy Unsafe one-click profiles with permanent security reductions, firewall/service/task suppression, managed hosts blocks, aggressive AppX removal, hibernation disable, privacy/performance rules, and optional display override reset.
- Added typed Critical contracts, exact preview receipts, operation-specific policy gates, UAC brokering, journals, dynamic compensators, conflict-aware restoration, and explicit acknowledgement for destructive profiles.
- Bumped the product and evidence baseline to WinCare 4.4.0.

## 4.3.0 - 2026-07-21

### Operations convergence

- Added a protected BITS download queue with one-to-eight mirrors, scheduled and due-job starts, bounded concurrency/retries/headers/bytes, policy-gated HTTP, pause/resume/cancel/reconcile, hash verification, same-directory staging, destination tombstones, post-commit verification, and dynamic cancellation recovery.
- Added local operations telemetry for CPU, memory, formatted disk throughput, network rates, process/thread/handle counts, top consumers, configurable alerts, protected bounded history, cancellation, and local export without remote telemetry.
- Added unified launcher indexing for bounded non-reparse Start-menu traversal, App Paths, Control Panel applets, safe Settings URIs, weighted local search, exact target hashes, and arithmetic parsing without evaluation or shell construction.
- Added Steam library/user/cloud-cache discovery, exact per-file backup manifests, ZIP admission, hidden-member rejection, Steam-process race prevention, provider-internal restore rollback, complete post-state verification, and defensive game-integrity findings without cheat execution.
- Added strict offline reduction profiles with exact AppX/feature identities, mounted read-write image requirements, policy gating, ResetBase denial, reversible feature changes, and explicit non-reversible package/component-store boundaries.
- Added revisioned workspace-layout catalogs, compare-and-swap saves/removals, strict ratio and identity validation, 100 ms title-regex timeouts, exact process/window/monitor binding, and per-window compensation.
- Expanded the transaction graph to 98 typed action contracts, 144 headless commands, and 44 routed menu actions.
- Expanded cumulative convergence evidence to 69 donors, 13,793 classified files, 326 semantic decisions, 164 composition groups, 224 target nodes, and 134 convergence test nodes.
- Added donor-wave tests for queue/store integrity, strict download limits, atomic replacement, local telemetry privacy, exact Steam archive membership, restore races, offline-profile admission, regex bounds, routing, and broker authority.



## 4.2.0 - 2026-07-21

### Productivity and support convergence

- Added governed system/display/away-mode power sessions using a dedicated integrity-bound host, revisioned cross-process records, heartbeat/expiry reconciliation, exact process identity, policy duration caps, runtime-issued stop compensators, and no hidden resident authority.
- Added DPI-aware monitor and window inventory, normalized ratio zones, exact HWND/process/start-time/title/bounds compare-and-swap, reversible topmost and bounds changes, explicit activation, and local modifier/button emergency release without global hooks.
- Added one-shot cursor pixel capture, normalized HEX/RGB/HSL/HSV records, named colors, a revisioned protected palette, exact rollback, corrupt-store mutation refusal, and bounded image/frame metadata inspection.
- Added local-first Markdown notes with strict metadata, atomic body/metadata snapshots, private-body redaction from ordinary views and reports, explicit local private search, stale-state protection, remove/restore compensation, and no cloud dependency.
- Added browser process/window/profile/extension summaries, local manifest permission-risk classification, bounded inventory, and explicit refusal to install or enable a browser extension merely to count tabs.
- Added remote-support application/process/service/listener/config-key inventories, value/secret redaction, Prepared/Active/Closed/Revoked/Expired consent records, expiry reconciliation, exact-process emergency termination, and local input recovery.
- Added Maintenance/Productivity/Support/Developer/Minimal workspace modes, deterministic bounded fuzzy command ranking, usage-aware ordering, explainable admission boundaries, and strict data-only extension command aliases.

### Safety, evidence, and validation

- Expanded the transaction graph to 85 typed action contracts, 114 headless commands, and 39 routed workspaces.
- Added fail-closed mutation reads for corrupt palette and consent stores, a 256 MiB image-file bound, decoded dimension/pixel bounds, and configured browser inventory limits.
- Added provider-internal rollback for window, note, palette, consent, and power-session failures before transaction completion can be journaled.
- Expanded cumulative convergence evidence to 52 donors, 9,004 classified files, 252 semantic decisions, 129 composition groups, 196 target nodes, and 104 convergence test nodes.
- Added wave-three tests for policy defaults, action/dispatch parity, host integrity, private-note behavior, snapshot tampering, durable-store corruption, image bounds, browser permissions, consent transitions, broker admission, UI/headless routes, and data-only extension aliases.

## 4.1.0 - 2026-07-21

### Governed expert operations

- Added system-managed, custom, and policy-gated disabled page-file modes with crash-dump compatibility checks, fixed-volume validation, growth-aware free-space checks, exact pre-state hashes, restart disclosure, provider-internal rollback, and executable recovery.
- Added authenticated, expiring maintenance modes for Defender real-time protection, shell SmartScreen, UAC, and the Windows Update service stack. Reductions are disabled by default, bounded by policy, require a reason/environment declaration, reject Tamper Protection bypass, schedule automatic restoration, and preserve exact Registry/service state.
- Added one-variable TCP experiments for receive-window auto-tuning, ECN, RSS, and RSC. Each experiment captures a bounded DNS/ICMP/TCP baseline, verifies the exact `netsh` token, applies one allowlisted value, repeats the measurement, and automatically restores regressions or cancellations.
- Added supported process instrumentation through bounded WPR/ETW capture, loaded-module/signature inventory, active ETW-session inventory, and Sysmon remote-thread evidence.
- Added defensive inventory for AppInit DLLs, IFEO debugger/verifier values, SilentProcessExit monitors, AppCert DLLs, LSA packages, Winlogon values, and permanent WMI consumers. Only exact allowlisted Registry surfaces can be quarantined; arbitrary injection, remote memory writes, global hooks, and payload execution remain denied.

### Safety and recovery hardening

- Expanded the transaction graph to 73 typed action contracts, 88 headless commands, and 33 routed workspaces.
- Bound page-file, security, TCP, and interception recovery descriptors to strict schemas, exact hashes, allowlisted targets, and compare-before-restore semantics.
- Made automatic security restoration independent of a particular installed module-tree hash so an in-place WinCare upgrade cannot strand a reduced protection state; authenticated record schema and exact current-state checks remain mandatory.
- Added exact delayed-auto-start capture for Windows Update services, explicit SmartScreen/UAC verification, cancellation checkpoints for network measurements and ETW capture, DNS timeouts, and a hard 25-minute network-experiment bound.
- Added provider-internal rollback for partial mutations and exact post-rollback verification before reporting recoverability.

### Evidence and validation

- Extended the cumulative evidence graph to 186 semantic decisions, 115 composition groups, 134 target nodes, and 90 test nodes while retaining complete classification for all 6,770 surfaces from 38 donors.
- Added expert convergence tests for default-deny policy, action/dispatcher parity, page-file bounds, expiring restoration, TCP allowlists/baselines, cancellation, ETW-only instrumentation, and injection API prohibition.


## 4.0.0 - 2026-07-21

### Cumulative peer convergence

- Re-audited both donor waves as one graph: 38 archives, 6,770 files, 181 semantic decisions, 111 composition groups, 122 target nodes, and 86 test nodes.
- Added explicit duplicate-archive accountability for repeated Rainmeter and ExplorerPatcher archives rather than inflating implementation claims.
- Preserved exact archive/file hashes, provenance, target ownership, negative invariants, recovery requirements, operator surfaces, and acceptance evidence for every material decision.

### New capability planes

- Added context-menu catalogs, bounded Registry-tree snapshots, exact compensators, modern menu-definition validation, and external customizer inventories.
- Added Microsoft-signed LGPO import with pre-import backup/internal rollback and Microsoft-signed Sysmon configuration plus event/state evidence.
- Added policy-gated mounted-image inventory and driver/package/feature servicing with healthy read-write mount enforcement and verification.
- Added local widget definitions/snapshots, configurable layouts, independent provider degradation, and exact local exports.
- Added bounded read-only Bluetooth/PnP classification, battery observations, and event evidence without pairing or driver mutation.
- Added protected maintenance-window state, closed transitions, exact record rollback, event evidence, metrics, and local Prometheus text export without a listener.
- Added strict data-only playbooks that compose validated WinCare plans and reject embedded scripts, macros, and arbitrary commands.
- Expanded policy, configuration, reports, dashboard, quick actions, settings, command palette, module exports, TUI routes, and headless JSON surfaces.

### Hardening

- Expanded the transaction graph to 64 action contracts, 71 headless commands, and 32 routed workspaces with exact parity checks.
- Bounded Registry capture/restore depth and entries, removed process-global counters, and strengthened nested snapshot validation.
- Added staged and flush-verified managed-file replacement with internal rollback if commit verification fails.
- Corrected maintenance rollback to restore the complete previous record instead of attempting a forbidden reverse state transition.
- Added provider-internal LGPO restoration when import or refresh fails before the outer transaction can persist a compensator.
- Required LGPO rollback to refresh policy and reproduce the exact pre-import policy-store hash before reporting recovery success.
- Added provider-internal cleanup of fresh Sysmon installs when configuration or service verification fails, while refusing to mislabel updates to pre-existing installations as fully reversible.
- Added exact before-state compare-and-swap and provider-internal rollback for offline feature mutation, plus verified removal of newly introduced non-protected offline driver packages after partial failure.
- Bound maintenance-window mutations to both store revisions and per-record hashes, emitted exact dynamic compensators, and rejected stale recovery attempts.
- Tightened widget and Bluetooth providers with bounded inventories, bounded event text, strict definition admission, unknown-widget rejection, canonical local exports, and no pairing or driver mutation.
- Required mounted images to be healthy and read-write for mutation and added package identity verification.

### Evidence, tests, and release

- Added cumulative evidence tests and second-wave convergence contract tests.
- Updated cross-file and convergence validators for all 38 donors and cumulative interfaces.
- Updated deterministic release, Windows CI/validation, SBOM, receipts, and provenance for 4.0.0.

## 3.0.0 - 2026-07-21

### Converged architecture

- Replaced the flat operation layer with schema-v3 typed actions/plans, 46 strict action contracts, policy risk floors, compatibility metadata, deterministic plan hashes, idempotency keys, pre/postconditions, bounded execution, cancellation, verification, and reverse-order compensation.
- Added schema-v4 hash-chained operation journals, canonical order-independent action/plan/event/final-record hashing, schema-v2 protected terminal receipts, interruption reconciliation, integrity-gated undo construction, and undo receipts bound to original and recovery operations. Legacy receipt formats remain historical-only and cannot authorize automatic undo.
- Added strict configuration/policy admission with user/session/organization/machine precedence and non-overridable disabled telemetry.
- Rebuilt UAC elevation as an authenticated, expiring, replay-protected typed-action handoff bound to action/operation identity, policy, helper, full module tree, state root and result path.
- Added a current-user authenticated read-only local broker with sequence-checked bounded result chunks.

### New product capabilities

- Added adaptive conditions, capability-aware menus, command palette, navigation persistence, reduced-motion, screen-reader, ASCII, high-contrast and monochrome modes.
- Added strict reversible privacy, Explorer, performance, gaming, developer, security and repair catalogs plus exact AppX review profiles.
- Added safe unattend analysis, system/user provisioning blueprints, exact AppX identities, WinGet package plans and reproducible hash-bound OEM provisioning kits.
- Added WUA COM search/history/download/install/hide/unhide/uninstall flows with per-item cancellation and restart results.
- Added WDAC XML/CIP validation, conversion, CiTool-only audit-aware deployment/removal, active-policy inventory and Code Integrity events.
- Added process/memory/CPU/AppContainer/credential-provider/signature diagnostics; Core Audio, brightness, Wi-Fi and privacy-safe clipboard controls; reversible process priority/affinity.
- Added BCD/WinRE diagnostics and recoverable BCD export; explicitly prohibited raw bootloader/EFI writes.
- Added shell/context-menu inventory and reversible state plans.
- Added strict bounded automation profiles, Task Scheduler integration and secret/recursion guards.
- Added data-only declarative extensions with exact manifests, hash/size/path/link/version/signer validation and recoverable install/remove plans.
- Hardened startup backups, application-data backups, quarantine, support bundles, driver exports, fleet/remote exports and cleanup paths against overwrite, traversal, reparse and tampering.

### Peer convergence and release evidence

- Investigated 18 supplied archives and classified all 1,005 donor files.
- Added 79 semantic decision records across 52 composition groups with exact evidence hashes, dispositions, target nodes, invariants, risks, operator surfaces and test links.
- Added independent convergence validation, peer synthesis, threat model, omission audit, language ownership, donor notice and release runbook.
- Added deterministic packaging, strict archive verification, SPDX SBOM, exact release manifest, source/evidence hashes, release receipt, provenance and Windows validation orchestration.
- Replaced legacy tests with focused contract, admission, safety/recovery, protocol/supply-chain, UI/release and convergence suites.

## 2.0.0 - 2026-07-20

- Added versioned transactions, policy controls, recovery, broader workspaces, enterprise/reporting surfaces, extension groundwork, package metadata and validation tooling.

## 1.0.0 - 2026-07-20

- Added the initial zero-dependency PowerShell TUI, application inventory/uninstall, cleanup, storage, startup, health, security, updates, network, reporting, installer and Windows CI.
